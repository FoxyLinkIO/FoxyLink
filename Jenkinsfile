pipeline {
    agent any
    
    stages {
        stage('QASonar') {
            
                try {
                    echo 'FoxyLink'
                } catch (e) {
                    echo 'Sonar status : ${e}'
                }
            
        }
    }
}
